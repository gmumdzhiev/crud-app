specVersion: "v1beta"
serviceId: "crud-app"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "crud-app-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
